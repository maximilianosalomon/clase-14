class Item {
  constructor(timestamp, nombre, descripcion, codigo, imagen, precio, stock) {
    // this.id = id;
    this.timestamp = timestamp;
    this.nombre = nombre;
    this.descripcion = descripcion;
    this.codigo = codigo;
    this.imagen = imagen;
    this.precio = precio;
    this.stock = stock;
  }
  // // metodos
  // setId(data) {
  //   const contenido =
  //   const ids = item.map((producto) => producto.id);
  //   const maxId = Math.max(...ids);
  // }
}
module.exports = Item;
